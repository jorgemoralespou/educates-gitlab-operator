[project]
name = "educates-gitlab-operator"
version = "1.0.0"
description = "GitLab Operator for Educates Workshops"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "kopf>=1.37.5",
    "kubernetes>=32.0.1",
    "pyyaml>=6.0.3",
    "requests>=2.32.5",
]
